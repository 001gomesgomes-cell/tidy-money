# Tidy Money — Budget Dashboard

Digital product launch experiment (US market, USD). Static landing page in `docs/` (GitHub Pages),
product generator in `build/`, deliverables in `product/`. Operating log: `DIARIO.md`, blockers: `PENDENTES.md`, results: `RELATORIO.md`.
